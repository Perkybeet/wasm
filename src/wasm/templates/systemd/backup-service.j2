{% import "_escape.j2" as esc %}
# WASM scheduled backup for {{ esc.line(domain) }}
# Generated by WASM

[Unit]
Description=WASM scheduled backup for {{ esc.line(domain) }}

[Service]
Type=oneshot
ExecStart=/usr/bin/wasm backup create {{ esc.line(domain) }} --include-databases --tags scheduled,auto
