/** The surface a settings section's content sits on, the same as the overview's panels. */
export const PANEL = "min-w-0 rounded-card border border-border bg-surface shadow-raised";

/** A text link in a section's body. */
export const LINK =
  "rounded-[4px] text-13 font-medium text-accent-fg hover:underline hover:underline-offset-2 focus-visible:outline-2 focus-visible:outline-focus";
